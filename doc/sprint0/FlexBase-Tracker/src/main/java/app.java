import View.LoginPage;
import View.NewHabitPage;

/**
 * This is where the app will run from
 * currently only running the login page as example
 */
public class app {
    public static void main(String[] args) {
        LoginPage app = new LoginPage();
        app.show();
    }
}
